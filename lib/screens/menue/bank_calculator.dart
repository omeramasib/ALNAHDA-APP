import 'package:flutter/material.dart';

class BankCalculator extends StatefulWidget {
  const BankCalculator({ Key? key }) : super(key: key);

  @override
  State<BankCalculator> createState() => _BankCalculatorState();
}

class _BankCalculatorState extends State<BankCalculator> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}